<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8"/>
  <style>
    body{font-family:Arial,sans-serif;margin:2em;}
    header{display:flex;align-items:center;justify-content:space-between;}
    .logo{height:60px;}
    table{width:100%;border-collapse:collapse;margin-top:1em;}
    th,td{border:1px solid #ccc;padding:8px;text-align:left;}
    .observaciones{margin-top:1.5em;font-size:0.9em;color:#333;}
    footer{margin-top:2em;font-size:0.8em;color:#666;text-align:center;}
  </style>
</head>
<body>
  <header>
    <img src="<%= logoUrl %>" class="logo" alt="Logo"/>
    <h1>COTIZACIÓN <%= numero %></h1>
  </header>

  <section>
    <h2>Datos del Cliente</h2>
    <p><strong>Nombre:</strong> <%= cliente.nombre %></p>
    <p><strong>Email:</strong> <%= cliente.email %></p>
    <p><strong>Teléfono:</strong> <%= cliente.telefono %></p>
  </section>

  <section>
    <h2>Detalle de Productos / Servicios</h2>
    <table>
      <thead><tr>
        <th>ID</th><th>Descripción</th><th>Cant.</th><th>Precio</th><th>Total</th>
      </tr></thead>
      <tbody>
        <% items.forEach(it => { %>
        <tr>
          <td><%= it.id %></td>
          <td><%= it.descripcion %></td>
          <td><%= it.cantidad %></td>
          <td>$<%= it.precio.toFixed(2) %></td>
          <td>$<%= it.total.toFixed(2) %></td>
        </tr>
        <% }); %>
      </tbody>
    </table>
  </section>

  <section>
    <h2>Totales</h2>
    <p>Subtotal: $<%= subtotal.toFixed(2) %></p>
    <p>Descuento: $<%= descuento.toFixed(2) %></p>
    <p>Impuestos: $<%= impuestos.toFixed(2) %></p>
    <p>Anticipo: $<%= anticipo.toFixed(2) %></p>
    <h3>Total Neto: $<%= total.toFixed(2) %></h3>
  </section>

  <section class="observaciones">
    <h2>Condiciones</h2>
    <p><%= observaciones %></p>
    <p><strong>Nota:</strong> Pagos con tarjeta con recargo del 3%.</p>
  </section>

  <footer>
    <p>Jotaye Group LLC – +1 305-417-2681 – sales@jotayegroupllc.com</p>
    <p>www.jotayegroupllc.com</p>
  </footer>
</body>
</html>
